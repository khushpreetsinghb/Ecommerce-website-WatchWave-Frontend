# Copy to `.env.local` for local development.
# Local backend URL (kept in sync with backend/.env PORT).
# In production (Vercel/Netlify dashboard) set this to the deployed
# Express API, e.g. https://watchwave-api.onrender.com (no trailing slash).
API_URL=http://localhost:8080
